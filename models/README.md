# Models Directory

把模型相關資產放在這裡：

- `models/adapters/`
  - 自訂 LoRA adapter
- `models/cache/huggingface/`
  - Hugging Face cache

`model_info.json` 內的 `lora_path` 會指向 `models/adapters/...` 這類 repo-relative 路徑。
